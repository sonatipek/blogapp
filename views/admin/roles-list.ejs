<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs', {title: "Role List"}) %>
</head>
<body>
    <!-- Navbar -->
    <%- include('../partials/navbar') %>

    <div class="container">
        <% if (action && action === "create") { %>
            <%- include('../partials/alert.ejs', {alertClass: "success", alertText: "Category Created!"}) %>
        <% } %>
        
        <% if (action && action === "update") { %>
            <%- include('../partials/alert.ejs', {alertClass: "warning", alertText: "Category Updated!"}) %>
        <% } %>

        <% if (action && action === "delete") { %>
            <%- include('../partials/alert.ejs', {alertClass: "danger", alertText: "Category Deleted!"}) %>
        <% } %>
    </div>
        
    <table class="table table-hover container mx-auto mt-3">
        <thead>
            <tr>
              <th scope="col">ID</th>
              <th scope="col">Role</th>
              <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            <% roles.forEach(role => { %>
                <tr>
                  <th scope="row">#<%= role.id %></th>
                  <td><%= role.role_name %></td>
                  <td>
                    <a href="/admin/roles/<%= role.id %>" class="btn btn-sm btn-outline-danger">Edit Role Name</a>
                </td>
                </tr>
             
            <% }) %>
        </tbody>
    </table>
</body>

</html>