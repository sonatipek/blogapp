<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs', {title: "User Login"}) %>
</head>
<body>
  <% if (typeof message != "undefined") { %>
    <div class="alert alert-warning text-center">
      <%= message %>
    </div>
  <% } %>

  <% if (returnUrl.returnUrl) { %>
    <div class="alert alert-warning text-center">
      Gitmek istediğiniz sayfaya gitmek için yetkiniz yok. Yetki doğrulaması için giriş yapın. <br> Yetki doğrulamasının başarılı olması halinde linke yönlendirileceksiniz.
    </div>
  <% } %>

    <form method="post" class="container mt-5 mx-auto">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="mb-3 col-8">
          <label for="user_email" class="form-label">Email address</label>
          <input type="email" class="form-control" id="user_email" name="user_email" aria-describedby="emailHelp">
        </div>

        <div class="mb-3 col-8">
          <label for="user_password" class="form-label">Password</label>
          <input type="password" class="form-control" id="user_password" name="user_password">
        </div>

        <button type="submit" class="btn btn-success">Login</button>
      </form>
</body>
</html>